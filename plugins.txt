# List of enabled plugins
# To enable plugin, simply enter its module name here:
#
# plugin_module
#
# If plugin is outside of python path, you can use the `some/path:module` syntax:
#
# /app/my/custom/plugin:plugin_module
#
# To enable multiple plugins, list their names in separate lines!

testplugin
/app/plugins:otherplugin