extend type Mutation {
    changeSettings(input: ChangeSettingsInput!): ChangeSettingsResult!
}

input ChangeSettingsInput {
    forumIndexHeader: String!
    forumIndexThreads: Boolean!
    forumIndexTitle: String!
    forumName: String!
}

type ChangeSettingsResult {
    errors: [Error!]
    settings: Settings
}