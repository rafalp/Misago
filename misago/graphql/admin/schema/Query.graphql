extend type Query {
    category(id: ID!): Category
    categories: [Category!]
    user(id: ID!): User
    users(filters: UsersFilters): UsersList
}

input UsersFilters {
    name: String
    email: String
    isActive: Boolean
    isModerator: Boolean
    isAdministrator: Boolean
}