extend type Query {
    categories: [Category!]
    user(id: ID!): User
    users(filters: UsersFilters): UsersList
}

input UsersFilters {
    name: String
    email: String
    isActive: Boolean
    isModerator: Boolean
    isAdministrator: Boolean
}