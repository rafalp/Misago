extend type Mutation {
    register(input: RegisterInput!): RegisterMutationResult!
}

input RegisterInput {
    name: String!
    email: String!
    password: String!
}

type RegisterMutationResult {
    user: User
    token: String
    errors: [Error!]
}