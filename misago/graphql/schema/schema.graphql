scalar Datetime

type Query {
    auth: User
    categories: [Category!]!
    settings: Settings
}

type Settings {
    forumName: String!
}

type User {
    id: ID!
    name: String!
    slug: String!
    email: String!
    isModerator: Boolean!
    isAdministrator: Boolean!
    joinedAt: Datetime
}

type Category {
    id: ID!
    name: String!
    slug: String!
}

type Thread {
    id: ID!
    name: String!
    slug: String!
}

type Post {
    id: ID!
}