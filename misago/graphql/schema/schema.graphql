scalar DateTime
scalar Generic

type Query {
    auth: User
    categories: [Category!]!
    category(id: ID!): Category
    thread(id: ID!): Thread
    user(id: ID!): User
    settings: Settings
}

type Settings {
    forumName: String!
}