type Category {
    id: ID!
    parent: Category
    children: [Category!]!
    depth: Int!
    name: String!
    slug: String!
    color: String
    icon: String
    banner: CategoryBanner
}

type CategoryBanner {
    align: String!
    background: String!
    height: Int!
    url: String!
}