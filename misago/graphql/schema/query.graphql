type Query {
    auth: User
    categories: [Category!]!
    category(id: ID!): Category
    thread(id: ID!): Thread
    threads(cursor: ID, category: ID, user: ID): ThreadsFeed
    user(id: ID!): User
    settings: Settings
}