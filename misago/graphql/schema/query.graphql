type Query {
    auth: User
    categories: [Category!]!
    category(id: ID!): Category
    thread(id: ID!): Thread
    threads(cursor: ID, category: ID, user: ID): ThreadsFeed
    updatedThreads(cursor: ID!, category: ID, user: ID): Int!
    user(id: ID!): User
    forumStats: ForumStats!
    settings: Settings
}