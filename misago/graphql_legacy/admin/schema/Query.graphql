extend type Query {
    auth: User
    category(id: ID!): Category
    categories: [Category!]
    user(id: ID!): User
    users(filters: UsersFilters, page: Int): UsersPage
}

input UsersFilters {
    name: String
    email: String
    isActive: Boolean
    isModerator: Boolean
    isAdmin: Boolean
}