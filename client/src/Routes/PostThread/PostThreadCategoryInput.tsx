import React from "react"

const PostThreadCategoryInput: React.FC = () => {
  return (
    <button className="btn btn-secondary-outline" onClick={() => {}}>
      Pick category
    </button>
  )
}

export default PostThreadCategoryInput
