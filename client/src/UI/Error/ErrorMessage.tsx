import { Trans } from "@lingui/macro"
import React from "react"

const ErrorMessage: React.FC = () => (
  <Trans id="generic_error.message">An unexpected error has occurred.</Trans>
)

export default ErrorMessage
